# T-0032/S01 configured native File/CSV consumer

Issue: [#25](https://github.com/bhind/emburk/issues/25). State: Done through PR #116.
Forecast: 8 SP. This slice advances stages 2–4, not the complete CSV parent.

## Authority

The owner requested execution through stage 7 and authorized routine testing,
commits, pushes and merges absent incidents. ADR-0017 admits the bounded actual
consumer after integrated parser and reference evidence. PM owns acceptance.

## Dependencies

T-0022/S01 PR #114 and T-0014/S01 PR #115 (c3304fc) are integrated. The latter
passed primary/independent Demo at a031fb8; all eight exact input/config/output
projections matched. Existing private schema/record/handoff and line consumers
remain regression dependencies, not full parent-contract acceptance.

## Branch and allowlist

Branch feat/t-0032-configured-csv. Rust Core Implementer owns only Cargo.lock,
crates/emburk-core/Cargo.toml, crates/emburk-core/src/lib.rs,
crates/emburk-core/src/yaml_profile.rs, crates/emburk-core/src/csv_stream.rs,
crates/emburk-core/src/configured_csv.rs, crates/emburk-cli/src/main.rs,
crates/emburk-cli/tests/configured_csv.rs and
tests/t0032_configured_csv_differential_test.sh. One mutation owner; reviewers
read-only. PM owns this packet, ADR-0017, STATUS/TODO/ROADMAP/COMPATIBILITY/
ARCHITECTURE, provenance index, T-0014 closeout and daily log. Existing logical
schema/record/batch/handoff and line-transfer files must remain unchanged.

The implementer returned the initial implementation and paused. PM then took
serial ownership of the same source allowlist for review fixes and the retained
differential driver; no concurrent tracked-file mutation occurred.

## Artifacts

Use saphyr-parser =0.0.11 with default features disabled and exactly the
eight-package graph/checksums from tools/parser-experiment/Cargo.lock. No new
dependency versions. The owner approved this graph's build-script/procedural
macro execution; the narrow production boundary is now ADR-0017. All original
Rust code; no upstream Java/Ruby implementation copying or translation.

Reference: T-0014-file-csv-oracle.md pins the executable, four bundled modules,
license/NOTICE inventory, official docs and source commits. Consulted material
is docs/manifest/license metadata and original black-box observations only.
Local runtime use, not redistribution; full security, SBOM and patent/FTO gaps
remain unreviewed. No new source or patented mechanism is adopted by implication.

## Acceptance criteria

Implement `emburk run CONFIG` for the exact observed explicit CSV option values
generated by tools/file-csv-oracle/run.py: in/out file, path_prefix, parser CSV,
UTF-8/LF/comma/double-quote escaping, skip_header_lines, ordered long/string
columns, formatter CSV header_line true/MINIMAL and exec max_threads=1,
min_output_tasks=1. Support arbitrary names/values in these selected domains,
not fixture-specific output constants. Full syntax validates before output.

Preserve raw ordered duplicates; selected skip_header_lines uses last value
(2 then 1 reference). Reject unsupported aliases/tags/options/types visibly.
Match eight oracle output-name/byte and exit projections exactly, including
malformed-long skip, missing-input no output, null vs quoted empty, multiline,
sentinel preservation and suffix 000.00.csv. Do not normalize unknown results.
Use one bounded CSV logical row and the private handoff, not whole-file loading.
Single-file profile only: reject multiple prefix matches before output.

Test config/record/column limits, invalid UTF-8, malformed quotes/row width,
unknown options, no overwrite, validation-before-output, and unchanged prior
CLI commands. Explicit native safety refusal outside the reference matrix is
not general parity. Existing generated outputs are rejected, not overwritten.

## Demo Command

`cargo fmt --all -- --check && cargo clippy --locked --workspace --all-targets -- -D warnings && cargo test --locked --workspace && bash tests/t0032_configured_csv_differential_test.sh && git diff --check`

Set EMBURK_REFERENCE_JAR to the pinned local executable and JAVA_HOME to Java 17.
The differential driver creates fresh private reference/native directories,
uses the actual built CLI and compares all eight cases. Retain raw results.

## Evidence class

Selected Differential (Embulk) plus Unit/Contract. Accepted 8 SP, integrated
as 66c6125e5783dd43bb5b3f7ab91c38bd5facc44c. Parent remains open in Backlog.

## Acceptance evidence

Exact Demo passed in primary and independent runs at
59a7cca60101efc17139bc838fe01e3cd070bb56, Rust/Cargo 1.98.1, macOS arm64,
Java 17: 81 tests passed, eight pre-existing intentional ignores; all eight
selected real reference/native exit, output inventory and byte comparisons
matched. Fmt, strict locked Clippy and diff-check passed. Security review
cleared after explicit non-optimizable driver guards; forced exit mismatch
under PYTHONOPTIMIZE returned 1 and retained an incomplete manifest.

Primary logs: /private/tmp/t0032-s01-primary.UdwrJB; stdout SHA256
9447552b88e3df3c7a91daab93d95713e7be51f2e9622fdb850d1638df81ef97,
stderr 4f7d9ee97817cb647d748c1f10363d9d710f45482ef7d6c8c9d868e2226f4f7c.
Independent: /private/tmp/t0032-s01-final-independent.RSbCch; stdout SHA256
c954bb1564566dc036df5a22baa909adb94b5fcf48df4f422f9db296d5b2809d,
stderr f44c40b8aaecc876b84078b9fc38249b6efe5d8fc48f51dcd2d3da74359c51fb.
Both exit 0; exit-file SHA256
9a271f2a916b0b6ee6cecb2426f0b3206ef074578be55d9bc94f6f3fe3ab86aa.
Independent manifest /private/tmp/emburk-t0032-differential-ikjarkh4/manifest.json
SHA256 ffca7589f93691e410c858f6cdda6399b13a8652a80ac03f715bbe393a4f7c14.

## Stop rule

Stop on new dependency graph, non-confined evidence writes, unexplained mismatch,
unexpected source/IP risk or unsafe output effects. No guessing behavior to get
a green comparison. Preserve partial work and logs; no points before integration.

## Non-claims

No full Embulk configuration/CSV/plugin certification, arbitrary output task
count, multiple input files, other types/encodings, transaction, atomic publish,
resume, compression, filter or performance claim. Later stages remain open.
