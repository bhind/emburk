# 2026-09-05

## Strategy and Governance Bootstrap

- Reframed Emburk as an independent Rust-native bulk data loader with a practical Embulk migration path.
- Replaced the unbounded “all plugins” goal with a pinned compatibility matrix and separate Hosted, Native, and Verified states.
- Selected a native File-to-File vertical slice as the first runtime milestone.
- Selected optional out-of-process JVM and JRuby hosts for unported artifacts, with Java preceding JRuby.
- Adopted a compact core, versioned plugin boundary, Arrow-compatible batches, correctness-first scheduling, and evidence-gated performance claims.
- Established repository-owned project memory, continuous Kanban, weekly review, Story Points, stable T-IDs, role boundaries, and a two-item active WIP limit.
- Adopted traceable, license-aware reimplementation rather than claiming formal clean-room separation.
- Created the private `Emburk Delivery` GitHub Project and linked it to `bhind/emburk`.
- Mirrored 8 unpointed epics and 44 pointed work items as 52 real Issues, with `T-0002` and `T-0011` in `Ready` and all other items in `Backlog`.
- Synchronized Status, Priority, Story Points, Initial SP, roles, parents, dependencies, workstreams, work types, evidence classes, and Demo Commands from `TODO.md`.
- Added Backlog, Kanban, Decisions, and Compatibility Project views. The Kanban view uses the Project's Status field as its lifecycle axis.
- Marked 13 explicit judgment-gate Issues with `type:decision`; nine Issues that cross license, notice, redistribution, patent, trademark, or dependency-audit gates carry `legal-review`.
- Aligned role display names with the Raveil pattern: Project Manager, specialized Implementers, Tester, Reviewers, and Librarian. Added Jitro and Vreji as call signs without placing call signs in Project role values.
- Added project-scoped Codex agent definitions, a two-thread local concurrency cap, and a minimal `CLAUDE.md` that delegates instruction authority to `AGENTS.md`.
- Started T-0002 and T-0011 on the shared initial integration branch `chore/t-0002-project-bootstrap`; the latter pins the core and SPI release commits. No external plugin artifact is admitted yet.
- Librarian review rejected the first T-0011 inventory because EEP-12 is absent from the pinned v0.11.5 commit and several provenance/non-claim fields were incomplete. The record now limits baseline EEP inputs to 3, 5, 6, 7, 9, and 10 and gives each consulted source an immutable locator and reuse decision.
- Committed the isolated bootstrap as `0a7052f` and opened [Draft PR #53](https://github.com/bhind/emburk/pull/53). T-0002 and T-0011 moved to `Review`; main remains unchanged.
- Returned T-0002 to `In Progress` for a bounded Rust smoke test after PR #53 became mergeable. Integration still awaits explicit approval to change main.
- Squash-merged [PR #53](https://github.com/bhind/emburk/pull/53) into `main` as `230e3af` after confirming a clean merge state, no failing checks, and no review blocker. Accepted T-0002 and T-0011 with their existing non-claims; the Rust bootstrap test passed once and the independent Librarian review remained valid.
- Returned dependent [PR #54](https://github.com/bhind/emburk/pull/54) to Draft after a direct base change exposed squash-ancestry conflicts. Its original base was restored without rewriting history; the brand-only change must be rebuilt from current `main` before review.
- Started owner-authorized T-0021/S01 as a one-point structural slice before the parent semantic dependencies. The slice permits only a virtual Cargo workspace, the `emburk-cli -> emburk-core` dependency direction, unsafe-code prohibition, and preservation of the bootstrap smoke test; all compatibility semantics remain deferred. Current Codex weekly telemetry reported a 10,080-minute window at 83 percent used and 17 percent remaining, above Emburk's 10-percent stop threshold. One low-cost implementation agent is permitted; no additional subagent is authorized for this slice.

## Evidence

- Started T-0012/S01 on `research/t-0012-config-presence` after the T-0011
  pins and the T-0021 workspace merge. The slice is limited to an independently
  authored local reference probe of absent, explicit-null, and defaulted typed
  configuration. Its provenance record preserves the no-copy and no-runtime-
  redistribution boundary; no runtime result or Emburk semantic claim exists.
- Blocked T-0012/S01 when the pinned public core POM graph compiled on Java 17
  but failed before its first case: `ModelManagerDelegateImpl` was absent. The
  missing artifact was not added. The clearing condition is a separate
  provenance/license/NOTICE-aware runtime-only admission decision, followed by
  the same nine-case run; this diagnostic does not verify any semantic behavior.
  The probe source hash and sanitized failure identity are retained in the
  T-0012/S01 provenance record; raw output and resolved classpaths remain local
  because they may contain personal paths.
- Reconciled T-0021/S01 after [PR #58](https://github.com/bhind/emburk/pull/58)
  merged as `5fe306a`: independent acceptance at `cf56841` passed format,
  Clippy, one unit test, workspace metadata, CLI smoke run, and diff check.
  This remains structural Unit/Contract evidence only. T-0021/S02's planning
  proposal was integrated by PR #60 as `341f285` after review at `5059900`.

- T-0021/S02 records the owner's strict observable-compatibility objective and
  a proposed Rust component structure, configuration/value fixture worksheet,
  lifecycle responsibilities, and failure/recovery table. Corrected ADR-0003's
  unsupported host-failure guarantee. No external implementation source was
  consulted or copied for this proposal; no differential result is claimed.
- The owner temporarily waived the weekly usage stop for this design work.
  This does not authorize purchasing capacity or consuming reset entitlements.

- Inspected Embulk core at commit `c1340f69320a60d57a9942d83bde063c6178393a` and public project documentation.
- Reviewed the governance structure of the neighboring Raveil-Clean repository at its `origin/main` reference.
- Verified the existing Emburk Rust bootstrap with formatting, Clippy, and tests before this documentation change.

## Remote coordination state

The private [Emburk Delivery Project](https://github.com/users/bhind/projects/2)
contains 52 items and remains a coordination mirror. Repository records remain
authoritative; any later partial remote update must be reconciled before a
lifecycle transition.
